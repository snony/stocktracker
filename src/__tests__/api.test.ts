// import { fetchAndUnpack } from 'api'

// const globalAny: any = global

describe('api', () => {
  it('should fetch and unpack correctly', async () => {
    // const response = await fetchAndUnpack('test')
    expect.anything()
  })
})
