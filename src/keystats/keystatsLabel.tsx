import React from 'react'

export const StatsLabel:React.SFC = ({ children }) => <span className="label label--small">{children}: </span>

export const StatsValue:React.SFC = ({ children }) => <span className="label">{children}</span>

